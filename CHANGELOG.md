# 更新记录

## 1.2.0

**修掉一个会丢内容的缺陷，并补上字号/颜色。**

### 缺陷：保存会删除表格等非段落内容

保存时的块列表完全取自编辑器 DOM，而 surface 会跳过非段落的 body 子元素——于是
「打开含表格的文档再保存」会把表格、块级内容控制、块级书签标记从文件里删掉。

1.0.0 与 1.1.0 都受影响。**只影响保存**：如果你的文档没有表格、内容控制这类结构，
就不会遇到；打开不保存也不会损坏文件。

- 保存改为以文件中解析出的结构为准，只把 DOM 采集到的可编辑段落替换回去；
- 无法用扁平文本重建的内容（超链接、图片、域、换行/制表符、修订）标记为只读：
  界面上显示但不可编辑，且无论 DOM 报告什么都逐字节保留原样；
- 零宽标记（书签、批注范围、权限、校对）原位放回，所以含书签的标题仍可编辑；
- 表格渲染成只读表格，内容控制显示其文字——不再有「看不见但存在」的部分。

### 新增

- 字号与颜色：解析并显示文档自身的 `w:sz` / `w:color`（此前一律按默认字号渲染，
  所见非所得），工具栏新增字号下拉、取色器与清除按钮；
- 改字号时 `w:szCs`（复杂文种字号）一起改，避免同段两种文种不一致。

### 测试

`npm test` 现在跑五段：`check` / `smoke` / `host` / `roundtrip`（47 项）/
`structure`（40 项）。`structure` 是防数据丢失的那一道，且**特意调用保存按钮真正
调用的那个函数**，不再绕过保存路径去测底层 emitter——早期测试正是因此漏掉了上面
那个缺陷。

## 1.1.0

- 改动过的段落继承原有格式：`pPr`（缩进、对齐、行距、自定义样式）逐字节复用，
  `pStyle` / `numPr` 定点替换，段内未改动的 run 逐字节复用；
- 修掉「只改格式（例如只加粗）被当成未改动而丢弃」的检测漏洞；
- `ping` 的版本号改为运行时读 `package.json`，可与安装的版本对照。

## 1.0.0

- 首个版本：右侧栏文档标签页里打开 `.docx`，编辑正文段落，加粗/斜体/下划线、
  标题级别、有序/无序列表，保存回原文件。
